import { _decorator, Component, Node } from 'cc';
const { ccclass, property } = _decorator;

@ccclass('Robotwake_up')
export class Robotwake_up extends Component {
    start() {
    /**
     * The `start` method is called before rendering the first frame.
     * This is where initialization code for the component should be placed.
     */

        // Initialize component state or properties here
    }

    update(deltaTime: number) {
        
    }
}


